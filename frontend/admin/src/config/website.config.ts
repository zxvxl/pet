import { images } from '@/assets';

export const websiteConfig = Object.freeze({
  title: 'NaiveUiAdmin',
  logo: images.logo,
  loginImage: images.loginImage,
  loginDesc: 'Naive Ui Admin中后台前端/设计解决方案',
});
